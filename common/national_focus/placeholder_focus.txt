focus_tree = {
	# Notes: cost = 7*n days
	id = mod_placeholder_focus

	country = {
		base = 0
		modifier = {
			add = 10
			tag = CRO
		}
		modifier = {
			add = 10
			tag = SER
		}
		modifier = {
			add = 10
			tag = BEL
		}
		modifier = {
			add = 10
			tag = AOI
		}
		modifier = {
			add = 10
			tag = BEL
		}
		modifier = {
			add = 10
			tag = LUX
		}
		modifier = {
			add = 10
			tag = EGY
		}
		modifier = {
			add = 10
			tag = GDR
		}
		modifier = {
			add = 10
			tag = GER
		}
	}

	focus = { # The democrat path
		id = mod_BRA_politics_democrat
		icon = GFX_focus_AFG_parliamentary_democracy
		x = 2 
		y = 0
		cost = 10 # n * 7 days

		#mutually_exclusive = { focus = mod_BRA_politics_oppose }
		completion_reward = {
			add_political_power = 40
		}
	}
}